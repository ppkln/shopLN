<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>แบบฟอร์มแก้ไขข้อมูลสินค้า</title>
        
    </head>
    <body>
      <% if (products) { %>
        <div class="container mt-3 p-3">
            <%- include('../views/header') %>
            <div class="row">
                <div class="col-md-8 mx-auto text-center  ">
                </div>
            </div>
            <div class="row">
                <div class="col-11 col-md-7 mx-auto mt-5">
                  <img class="card-img-top" src="/images/products/<%= products.productImages %>" alt=""  >
                    <div class="card-body">
                        <form class="form-horizontal" method="POST" action="/editProductDB" >
                            <div class="form-group">
                                <label class="control-label col-sm-2">รหัสสินค้า</label>
                                <div class="col-sm-10">
                                  <input type="text" class="form-control" name="productId" value="<%= products.productId %>">
                                </div>
                              </div>
                            <div class="form-group">
                              <label class="control-label col-sm-2">ชื่อสินค้า</label>
                              <div class="col-sm-10">
                                <input type="text" class="form-control" name="productName" value="<%= products.productName %>">
                              </div>
                            </div>
                            <div class="form-group">
                                <label class="control-label col-sm-2">ราคา</label>
                                <div class="col-sm-10">
                                  <input type="text" class="form-control" name="productPrice" value="<%= products.productPrice %>">
                                </div>
                            </div>
                            
                            <div class="form-group">
                                <label class="control-label col-sm-2">รายละเอียด</label>
                                <div class="col-sm-10">
                                    <textarea class="form-control" cols="30" rows="4" name="productDescription" ><%= products.productDescript %></textarea>
                                </div>
                            </div>
                            <div class="form-group">
                              <input type="hidden" name="edit_idPro" id="edit_idPro" value="<%= products._id %>">
                              <div class="col-sm-offset-2 col-sm-10">
                                <button type="submit" class="btn btn-primary">บันทึก</button>
                                <a class="btn btn-success " href="/manage">หน้าจัดการสินค้า</a>
                              </div>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
        <% }%>
    </body>
</html>
