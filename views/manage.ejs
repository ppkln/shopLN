<!DOCTYPE html>
<html lang="en">
<link>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Manage Page</title>
</head>

<body>
    <div class="container mt-3 p-3">
        <%- include('../views/header') %>
        <h2>จัดการสินค้า</h2>
        <p> <a href="/insertProductform" >เพิ่มสินค้า</a> ลบ แก้ไขข้อมูลในฐานข้อมูล <a href="/logout">ออกจากระบบ</a></p>           
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>ชื่อสินค้า</th>
                    <th>รหัสสินค้า</th>
                    <th>ราคา</th>
                    <th>รายละเอียด</th>
                    <th>ลบ</th>
                    <th>แก้ไข</th>
                </tr>
            </thead>
            <tbody>
                <% Object.values(products).forEach(function(item){%>
                <tr>
                    <td><%= item.productName %></td>
                    <td><%= item.productId %></td>
                    <td><%= item.productPrice %></td>
                    <td><%= item.productDescript %></td>
                    <td><a class="btn btn-danger" href="/delete/<%= item._id %>" onclick="return confirm('ต้องการลบข้อมูลนี้หรือไม่')">ลบ</a></td>
                    <td>
                        <form action="/edit" method="POST">
                            <input type="hidden" name="edit_id" id="edit_id" value="<%= item._id %>">
                            <button class="btn btn-primary" type="submit" >แก้ไข</button>
                        </form>
                    </td>
                </tr>
                <%}) %>
            </tbody>
        </table>
    </div>
</body>
</html>